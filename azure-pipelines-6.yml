

pool:
  vmImage: ubuntu-latest

jobs:
  - job: Foo
    steps:
      - script: echo "Foo"
        condition: always()
  - job: Baar
    
    steps:
      - script: echo "baar"
  - job: RA
    
    steps:
      - script: echo "RA"